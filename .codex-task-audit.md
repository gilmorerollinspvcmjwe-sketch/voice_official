Task: Find all pages that have routes registered but NO navigation entry in the Navbar.

Steps:
1. Read src/App.tsx and extract all Route paths
2. Read src/components/layout/Navbar.tsx and extract all navigation links (navItems array + dropdown items + right-side links like login/signup)
3. Compare and list pages that:
   - Have a Route in App.tsx
   - Are NOT linked from Navbar (not in navItems, not in dropdowns, not in auth links)
   - Note: sub-routes like /product/features are acceptable to skip if they have a parent route in nav
   - Flag: independent top-level pages without any nav entry (like /tts-demo)
4. Also check: Are there pages in src/pages/ that are not even routed in App.tsx?

Output format:

## Pages with Routes but NO Nav Entry
| Route | Component File | Type | Notes |
|-------|---------------|------|-------|

## Pages in src/pages/ but NOT Routed
| Component File | Notes |
|---------------|-------|

IMPORTANT: Do NOT modify any code files. Only read and report.
