Task: Add navigation entries for ALL pages that have routes but no nav link anywhere on the site.

Project: C:\Users\13609\.openclaw\workspace\voice-ai-website

Read these files first:
- src/components/layout/Navbar.tsx (main nav)
- src/components/layout/Footer.tsx (footer nav)

## Part 1: Navbar additions

### 1. /tts-demo (key feature page, completely hidden)
Add to the top-level navbar as a new nav item.
- Label: "AI Voice" (English) / "AI音色" (Chinese)
- Icon: Headphones from lucide-react
- href: '/tts-demo'
- Use getLocalizedPath() like other items
- Place it after "Demo" nav item

### 2. /demo/effects
Add to the Demo dropdown menu.
- Label: "Effects" (English) / "特效体验" (Chinese)
- Icon: Sparkles from lucide-react (optional, only if dropdown items have icons)
- href: '/demo/effects'

## Part 2: Fix broken nav link

### 3. /solutions/survey
The Navbar has a nav link to '/solutions/survey' but NO corresponding Route in App.tsx.
- Check if there's a survey-related page component in the project
- If not: create a simple placeholder page at src/pages/solutions/Survey.tsx that says "Coming Soon" or redirects to a relevant page
- Add the route to App.tsx

## Part 3: Footer additions (legal pages)

### 4. Legal pages in Footer
Read Footer.tsx. If it does not already have links to legal pages, add them:
- /legal/privacy - "Privacy Policy" / "隐私政策"
- /legal/terms - "Terms of Service" / "服务条款"  
- /legal/gdpr - "GDPR" / "GDPR合规"

These go in the footer (standard practice for legal links).

## Rules:
- Read existing Navbar.tsx and Footer.tsx FIRST to understand the structure
- Use the same coding style, same CSS classes, same patterns as existing code
- Import any new icons from lucide-react (check what's already imported)
- Use getLocalizedPath() for all hrefs
- For Chinese labels: use the i18n system if Footer supports it, otherwise hardcode bilingual text
- After all changes, run: npx tsc --noEmit to verify TypeScript compiles
- Do NOT modify package.json or add dependencies
